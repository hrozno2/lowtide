# samples

This folder is ignored by git except for this file.

The Home window ships its own sample document (Sample Documents ▸ The
Lighthouse Keeper), so nothing here needs to be published. Keep working drafts
somewhere outside the repository.
